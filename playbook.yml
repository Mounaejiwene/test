- name: Déploiement de Nginx
  hosts: web
  become: true
  tasks:
    - name: Mise à jour des paquets
      apt:
        update_cache: yes

    - name: Installer Nginx
      apt:
        name: nginx
        state: present

    - name: Ajouter page HTML personnalisée
      copy:
        dest: /var/www/html/index.html
        content: "<h1>Déploiement réussi via Jenkins + Ansible 🎉</h1>"
