- name: Déploiement dans cible1 depuis Jenkins
  hosts: localhost
  become: true
  tasks:
    - name: Écrire la page HTML dans cible1
      command: >
        docker exec cible1 bash -c "mkdir -p /var/www/html &&
        echo '<h1>Déploiement réussi via Jenkins + Ansible 🎉</h1>' > /var/www/html/index.html"
      delegate_to: localhost
